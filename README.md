# Python Datenspeicher & Textverarbeitung

Übungen, Kapitel und Transferaufgaben zu Datenspeicherung und Textverarbeitung
aus dem Python-Modul meiner Weiterbildung zur **KI-Entwicklung**
(Juni – Dezember 2025).

## Inhalte

| Ordner | Beschreibung |
|---|---|
| `Kapitel8` | Web-Scraping – Live-Suche nach Schlagwörtern auf einer Nachrichtenseite |
| `Kapitel9` | Textverarbeitung – Glückskeks-Generator und regelbasierte Spracherkennung via Regex |
| `PythonUebung8501` | Datenspeicherung – Personendaten in Textdatei speichern und laden |
| `PythonUebung8502Datenspeicher` | Studentendaten mit strukturiertem Format speichern und parsen |
| `PythonUebung8702Datenspeicher` | JSON-Serialisierung von Listen und Tupeln mit UTF-8 Encoding |
| `PythonUebung8703Datenspeicher` | JSON-Datei aktualisieren mit `seek` und `truncate` |
| `PythonUebung9301Textverarbeitung` | String-Methoden, Unicode-Zeichen und JSON-Speicherung |
| `PythonUebung9302Textverarbeitung` | Tagebuch – Wörter zählen, ersetzen und als JSON speichern |
| `PythonUebung9701Textverarbeitung` | Unicode-Text bearbeiten – suchen, ersetzen und speichern |
| `PythonUebung9702Textverarbeitung` | String-Methoden mit JSON und Dateioperationen |
| `Transferaufgabe9701` | Unicode-Textanalyse – Regex-Filter, Wortzählung, JSON-Pipeline |
| `Transferaufgabe9702Textverarbeitung` | Produktliste mit MwSt-Berechnung – Regex-Parsing und Dateiverarbeitung |
| `CaseStudy8701` | Adressbuch – interaktive JSON-basierte Kontaktverwaltung |

## Highlights
- Adressbuch mit persistenter JSON-Speicherung und interaktivem Menü
- Spracherkennung via Regex-Häufigkeitsanalyse
- Unicode-Verarbeitung mit mehrsprachigen Texten
- Robuste Fehlerbehandlung bei allen Dateioperationen
- Web-Scraping mit `urllib`

## Technologien
- **Sprache:** Python 3
- **Bibliotheken:** json, os, re, urllib
- **IDE:** PyCharm

## Weiterbildung
Dieses Repository ist Teil der Weiterbildung **KI-Entwicklung** (Juni – Dezember 2025).
